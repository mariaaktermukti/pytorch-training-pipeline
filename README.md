# pytorch-training-pipeline
This project implements a PyTorch-based neural network training pipeline for binary classification. It includes model building, forward pass, loss computation, backpropagation, optimization, and evaluation. The model is trained for 100 epochs, and performance is measured using accuracy on train and test data.
